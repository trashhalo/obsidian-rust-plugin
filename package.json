{
  "name": "obsidian-extract-url",
  "version": "0.1.0",
  "description": "Extract url converting content into markdown",
  "main": "main.js",
  "scripts": {
    "build": "microbundle build --target node -f cjs -o main.js --alias readable-stream=stream index.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "microbundle": "^0.13.0"
  }
}
